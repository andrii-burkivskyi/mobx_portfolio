export const DEFAULT_OBJECT = {};
export const DEFAULT_ARRAY = [];
export const NULL_CHAR = "\0";
export const DEFAULT_FUNCTION = () => {};
export const DEFAULT_ASYNC_FUNCTION = () => new Promise((resolve) => resolve());
